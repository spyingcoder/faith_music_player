name: faith_music_player
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_audio_query: "^0.3.5+6"
  # A Flutter plugin, Android only at this moment, that allows you query for audio 
  # metadata info about artists, albums, songs audio files and genres available on
  #  device storage. All work is made using Android native MediaStore API with 
  #  ContentResolver API and query methods run in background thread
  audio_manager: ^0.8.2
  #A flutter plugin for music playback, including notification handling.

  cupertino_icons: ^1.0.2
  splashscreen: 
  font_awesome_flutter:
  animated_text_kit: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/


  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
